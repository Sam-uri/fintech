spring:
  application:
    name: FintechApp
    env: dev

  datasource:
    url:
    username:
    password:

logging:
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG

  jpa:
    hibernate:
    #      ddl-auto: update
    show-sql: true

server:
  servlet:
    port: 8080
    contextPath: /fintech

jwt:
  secret:
  access:
    expiration:
    header:
    prefix:
    refresh-token-expiration:
